# Anthropic — already in use
ANTHROPIC_API_KEY=sk-ant-...

# Stripe — already in use (LIVE mode)
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Resend — already in use for lead capture + magic-link email delivery
RESEND_API_KEY=re_...

# Upstash Redis — already in use for usage tracking / rate limits
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...

# Site URL
NEXT_PUBLIC_SITE_URL=https://hiring.productions

# Admin token (gates /admin route)
ADMIN_TOKEN=...

# ----------------------------------------------------------------------------
# Supabase (NEW — Stage 1 of platform pivot, per PLATFORM-VISION.md)
# ----------------------------------------------------------------------------
# Get these from: Supabase project → Settings → API
#   - NEXT_PUBLIC_SUPABASE_URL is the "Project URL"
#   - NEXT_PUBLIC_SUPABASE_ANON_KEY is the "anon public" key (safe to expose)
#   - SUPABASE_SERVICE_ROLE_KEY is the "service_role secret" (NEVER expose)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
